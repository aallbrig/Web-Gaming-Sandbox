apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.api.app }}
spec:
  selector:
    app: {{ .Values.api.app }}
  ports:
    {{- range .Values.api.service.ports }}
    - protocol: {{ .protocol }}
      port: {{ .port }}
    {{- end }}
